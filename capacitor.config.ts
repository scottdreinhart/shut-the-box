import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.shutthebox',
  appName: 'Shut the Box',
  webDir: 'dist'
};

export default config;
